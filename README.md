# libSuperCpp4Win
精C++模块<br>
```
  您是否因为重复造轮子而烦恼呢？ 您是否在急于测试时，发现得从头写一个测试demo而烦恼呢？没关系，有精C++模块，解决您的麻烦，赶快下载吧(现在第一版还未开发完成)<br>
目前计划: TCP、HTTP(S)、二进制操作(getBit、setBit等)、普通内存读写模板类(ReadProcessMemory、WriteProcessMeory)、指针内存读写模板类(方便您的dll操作主进程内存)、DLL注入类等
下阶段目标: 自写string(更加方便的进行一个字符串的操作)
以及未来的其他干扰模块(无jcc指令实现分支)等
```
